[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput --settings=bibliotheque.settings_prod"]

[start]
cmd = "gunicorn bibliotheque.wsgi:application --bind 0.0.0.0:$PORT"
